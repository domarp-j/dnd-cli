# TODOs

- AI support!
- Support autocomplete of targets by name OR id
- Need to support mobs
- Consolidate `save` and `game` commands
- Bug with autocomplete. Navigating with arrows creates a new line, making for a confusing UX.
- Add a `reset` command to clear the game board. Prompt for confirmation. Include alias `clear`.

